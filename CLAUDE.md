# ドローン操作練習

家にあるトイドローン (E88/E99 系の折りたたみ機) を、部屋の中で
**操縦者から見た視点**で練習するシミュレータ。iPhone の Safari で動く。

実機がうまく飛ばせない理由は、だいたいこの 3 つ。ここを叩き込むのが目的:

1. **傾き = 加速度。** 前に倒すと「前に進む」のではなく「前に加速し続ける」。
   止めるには逆に倒す (当て舵)
2. **対面だと左右が逆。** 機体がこちらを向いていると、右に倒すと自分から見て左へ行く
3. **戻す癖がない。** 入れっぱなしにすると壁か天井に当たる

FPV (機体のカメラ視点) にはしない。上の 3 つはどれも操縦者視点でしか練習にならない。

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト
npm run test:bundle # 1 枚にまとめた版を、同じテストにかける
npm run test:all   # 上の 3 つ全部
npm run bundle     # dist/dorone.html を作る (配布・Artifact 用)
npm run icon       # apple-touch-icon.png を作りなおす
```

`npm run test:ui` は本物のブラウザを立ち上げ、**指の操作をそのまま再現する**
(マルチタッチも)。画面を触ったときの不具合はこちらでしか捕まらない。
変更したら必ず両方流す。`--slow` を付けると CPU を 4 倍遅くしたときも測る。

playwright はグローバルにあれば拾う。無ければ `npm i -D playwright`。

## 構成

```
index.html / styles.css   画面
core.js                   物理と 3D の計算。DOM を触らない
tasks.js                  練習課題・採点・失敗原因の診断。DOM を触らない
sound.js                  Web Audio。何を鳴らすかは core が決める
app.js                    操作と描画
core.test.js              core のテスト (node --test)
tasks.test.js             tasks のテスト (node --test)
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
tools/bundle.js           全部を 1 枚の HTML にまとめる
tools/make-icon.js        apple-touch-icon.png を作る (iOS は SVG 不可)
```

ファイルはすべて直下。スマホやタブレットからでも 1 ファイルずつ差し替えられるようにするため。

`window.__app` はテストから中身をのぞく入口。`simulate()` で時間を早送りでき、
`bench` から描画などを部品ごとに計測できる。

---

## 決めごと

### 座標と単位

x = 右、y = 上 (高さ)、z = 奥。単位は m / s / rad。
機首方位 `yaw = 0` で機首は +z (部屋の奥) を向き、増やすと右に回る。
部屋は 5.2m × 6.0m × 2.4m。操縦者 (カメラ) は (0, 1.55, -0.75) に立っている。

### 機体のモデル (手持ちの機体に寄せてある)

- **気圧センサーの高度維持あり。** スロットルは「上下の速度」の指示。戻すとその高さで止まる
- **自動水平 (アングルモード)。** スティックを戻すと機体は水平に戻る。
  ただし水平に戻っても**止まらない**
- **GPS も光学センサーもない。** トリムずれで一方向に流れ続ける。
  ここが実機で一番効くクセなので、必ず入れる
- 高度維持を切る「上級」モードもある。そのときスロットルは推力そのものになり、
  スティックはバネで戻らなくなる (実機の送信機と同じ)
- **機体は球ではなく平たい円盤** (直径 32cm・厚み 7cm)。球で当たり判定をすると
  厚みが直径ぶんあることになり、テーブルの下をくぐれなくなる
- **電池は走行をまたいで持ちこす。** ホバリングで約 7 分。上げ下げや傾きが多いほど
  早く減る。残り 7% で、操作にかかわらず勝手に降りはじめる (実機と同じ)

実測した数字 (`npm test` が見張っている):

| | |
| --- | --- |
| 最大の傾き 20° での終端速度 | 約 2.2 m/s |
| 全開から手を離して止まるまで | 約 2.5 秒 / 1.5m 惰性で進む |
| ワンキー離陸の落ちつく高さ | 約 1.1m |
| 無操作で 60cm の輪から出るまで (ふつう) | 平均 9 秒 |
| 電池 (ホバリング) | 約 7 分。派手に飛ばすと 1.4 倍以上早い |
| テーブルの下のすき間 | 34cm (機体の厚み 7cm) |

### 立ち位置 (カメラ)

画面の空いている所をなぞると、操縦者が歩く。地図と同じで「世界をつかんで引っぱる」向き。
`app.cam.pos` と `env.room.pilot` は**同じオブジェクトを指す**ので、動かすと
対面の判定 (`Tasks.isFacingPilot`) もそちらを向く。これは正しい:
実機でも「自分が動いて対面を避ける」のが定石なので、練習になる。

`Core.clampPilot()` が壁と家具から押し出す。人は半径 0.32m の円。
高さ 0.6m 未満のもの (コーンなど) はまたげる。

### 場所は 2 つ

- **部屋** (`createRoom`) … 5.2m × 6.0m × 2.4m。壁・天井・家具あり。課題はここ
- **広場** (`createField`) … 16m × 16m × 6m、`open: true`。壁も天井もない。
  端や上に当たっても墜落せず、そっと止まるだけ (`state.atLimit`)。
  目印の三角コーンは**千鳥に置く**。一直線だと操縦者から見て重なって
  「トーテムポール」になる

広場は部屋より**軽い**。壁 3 枚・天井・家具 30 パーツを描かなくなるため。
実測 (CPU 4 倍遅い): 描画 3.62ms → 2.36ms、1 フレーム 26.2ms → 22.4ms。

高さの上限を 6m にしてあるのは、それ以上あげると機体が数画素になって
何をしているか読めなくなるため。

`Tasks.prepare()` が `task.field` を見て場所を差し替える。

### 課題

10 個。先頭が「◎ 広場で自由に飛ぶ」(採点も時間切れもない)。
残り 9 個は実機でつまずく順。①〜⑦ が基本、⑧⑨ が応用。
課題ごとに必要なもの (荷物・風) は `Tasks.prepare()` と task のフラグが用意する。
アプリとテストで同じ道を通すため、ここを分けない。

- **⑨ 荷物を運ぶ** — 紐で吊った箱は振り子。機体の加速で遅れて振れ、
  その張力が機体を引っぱり返す。急に動かすと振り回される

**自由に飛ぶモード (`kind: 'free'`) は終わらない。** 目標も時間切れもなく、
ぶつけても 1.8 秒で置きなおして続く。採点も結果画面も出さない。

**夜は課題ではなく「設定」。** どの課題でも灯りを消せる (HUD の月ボタン、または設定)。
課題ごとに固定したいときは task に `night: true` を付ける。

**猫はいったん外している。** 物理 (`createCat` / `updateCat`)・描画 (`drawCat`)・
テストはそのまま残してあるので、`TASKS` に `cat: true` の課題を足せば戻る。

### ゲート (輪) の見せ方

**「いま狙う輪」だけがはっきり分かること**が最優先。細い線 1 本だと、
どこが穴でどこが枠なのか読めない。

| | 描き方 |
| --- | --- |
| いまの輪 | 帯を塗る + 内外にふち + ゆっくり明滅 + 大きい番号 |
| 次の輪 | 帯を塗る + 外のふちだけ |
| その先・くぐった輪 | 細い線 1 本 |

- 帯を塗るのは高い。遅い端末では 6 つ塗るだけで 1 フレームに 2.6ms 乗る。
  **塗るのは 2 つまで**にして、残りは線にする
- 画面の外にある輪は、端に三角の矢印と番号を出す。
  「どっちを向けばいいのか分からない」がいちばん困る
- 見えているかの判定に余白を入れないこと。入れると輪が映っているのに
  矢印も出て、番号が二重になる
- **同じ場所に輪を 2 つ置かない。** ⑦ の 8 の字は、交差点に 2 つ重ねていて読めなかった。
  手前と奥の 2 つの輪を作り、交差は「輪と輪のあいだ」で起こす
- コースは部屋の奥に置く。手前すぎると輪が画面いっぱいになって形が見えない

### 航法灯 (前が白・後ろが赤)

実機と同じ決まり。**カメラに向いている側だけを光らせる。**
裏側の灯りまで描くと、どちらを向いていても赤と白が両方見えて、向きが読めなくなる。
機首ベクトルとカメラ方向の内積で、前後の明るさをなめらかに切りかえる。

### 昼と夜

`PALETTE.day` / `PALETTE.night` の 2 つだけ。色は必ずここから引く
(`rgba(P.goal, 0.5)` のように)。3D 側にベタ書きの色を置かないこと。
夜は課題のフラグ (`night: true`) と設定のどちらでも入る (`app.night`)。

- **昼ははっきり明るくする。** 端末がダークモードだと、少し明るいだけでは
  昼に見えない。実測で 3D の平均が 470 (夜は 53) くらいの差をつける
- 家具の色は暗い部屋に合わせて選んであるので、`P.furniture` の倍率 (昼 1.55) で
  持ち上げる。`shadeColor` は 255 で頭打ちにする
- 明るい画面では白い文字が読めない。**HUD の上下に薄い暗幕**を敷く

- 暗いと**影が見えない**ので、代わりに機体の LED が足もとの床を照らす。
  これが高さを読む手がかりになる
- にじみは「外は薄く広く、中は濃く小さく」。逆にすると霧のように見える

### 音

何を鳴らすかは `core.audioParams()` が決める (node でテストできる)。
`sound.js` は「その値をどう音にするか」だけ。

- モーターは 4 つの発振器をわずかにずらす。実機の「うなり」が出る
- **iOS は指で触るまで音を出せない。** 最初のタップで `Sound.start()`
- 音は飾りではない。実機でも**出力は音で読んでいる**

### 描画

Canvas 2D だけ。3D は「点を回して透視投影 → 奥から順に塗る」。ライブラリは使わない。

- 部屋の外殻 (床・天井・壁) を先に描き、そのあと中のもの (家具・輪・機体) を
  奥行き順に描く。中にいるので、この順なら必ず正しくなる
- **影は必ず描く。** 画面で高さを読む唯一の手がかり。
  1 枚のべた塗りではなく、濃さを変えた輪を 3 枚重ねる。高いほど広がってぼやける
- 家具の足もとを暗くする。置いてある感じが出て、床との境目が読める
- **家具は箱の組み合わせで作る。** 1 つの箱では「ただの直方体」で何か分からない。
  ソファ = 台座 + 背もたれ + 肘掛け 2 + 座面 2、本棚 = 本体 + 天板 + 棚板 + 本、
  観葉植物 = 鉢 + 幹 + 葉を数枚。見た目と当たり判定は分けない (パーツごとに当たる)
- **箱の中に箱を入れない。** 奥から順に塗るだけなので、すっぽり入った箱は必ず隠れる。
  棚板のように「前だけ出す」のは正しい。テストで見張っている
- **細かい中身は箱で作らず、面に色を塗る。** 本棚の本がそうしてある。
  小さい箱を並べると飛び出して見えるうえ、面の数も増える。
  `room.decals` は「面に貼るだけの平らな板」。描くが、当たり判定は持たない
  (当たるのは本体の箱のほう)。ふちも描かない
- 画面で数画素にしかならない面は描かない。パーツを増やすとここが効く
- 小さい面のふち取りも描かない。面の数が多いと、ふちが塗りと同じくらい高い
- 画角は「見える範囲の対角」で決める。縦だけ・横だけで決めると、
  縦長の画面で極端な望遠になる
- カメラは目で追うようにゆっくり機体を向く。デッドゾーンの中は動かさない
  (動いている感じを残すため)。ただし**最後に固い上限**をかけて画面から出さない
- 影・機首の矢印・荷物・猫・ゴーストは、家具と同じ列に入れて奥行き順に描く

---

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **ロジックを画面から切り離す。** `core.js` と `tasks.js` は DOM を触らない。
  だから物理も 3D の計算も node でテストできる
- **ビルドを挟まない。** 素の HTML / CSS / JS

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  ここで作ったときも、テスト 31 件が全部通った状態で
  「機体が画面に映っていない」「影が家具に隠れる」が残っていた
- **体感の問題は計測する。** 憶測で直さない
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅と、遅い端末でも見る。** `--slow` で CPU を 4 倍遅くする

### 3. テストプレイしてもらう

1. `npm run bundle` で `dist/dorone.html` を作る
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm run test:all` を流す**

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 縦画面が極端な望遠になる (水平 31°) | 画角を縦だけで決めていた。縦長の画面では横がすごく狭くなる | 「見える範囲の対角」で決める |
| 速く動かすと機体が画面から出る | カメラの追従がゆっくりなので、角速度が大きいと追いつけない | デッドゾーンとは別に、**固い上限**を最後にかける |
| 離陸すると機体が画面の上に飛ぶ | 最初に床のドローンを見おろしていて、上がるほど遅れが出た | 最初から「これから浮く高さ」を見ておく |
| 機体の手前にいるのに影が家具の裏に回る | 影を床と一緒に描いていた | 影と機首の矢印は、家具と同じ列に入れて奥行き順に描く |
| ヒントの吹き出しが機体を隠す | HUD の上に出していた | スティックのすぐ上に出す。HUD 全体にスティックぶんの余白を取る |
| 明るい昼にすると HUD の文字が読めない | 背景が明るいので、白い文字が沈む | HUD の上下に薄い暗幕 (グラデーション) を敷く |
| その暗幕を敷いたら、今度は文字まで暗くなった | 絶対配置の要素は、ふつうに並んだ中身より**後**に描かれる。暗幕が文字の上に乗っていた | 親を重ね順の基準にして (`z-index: 1`)、暗幕を `z-index: -1` で後ろに回す |
| 採点のバーが伸びない | `span` はインライン要素。`width`/`height` の % が効かない | `display: block` を付ける |
| 進み具合のバーを隠したのに枠が残る | 中身 (fill) だけ `hidden` にしていた | 枠ごと隠す。テスト側も、幅 0 の要素は「見えない」と判定されることに注意 |
| やり直した直後にエラーで落ちる | 結果画面を遅らせて出すあいだに次の走行が始まり、まだ採点していない走行を表示しようとした | どの走行の結果かを覚えておき、入れかわっていたら出さない |
| 離陸した最初の 1 フレームで「着地した」ことになる | 地上 (y=0) から浮いた瞬間に床との接触判定が立つ | 一度しっかり浮いた機体だけが着地できる (`airborne`) |
| 天井に描いた照明が「描画の失敗」に見える | 画面の上端は遠近が強くかかり、四角がゆがむ | 天井は素のままにする |
| 遅い端末で 21fps しか出ない | canvas の画素数が支配的 (自分の JS は 3ms しか使っていなかった) | fps を見て**描く細かさを自動で下げる** (2x → 1.5x → 1x)。実測 47ms → 17ms |
| 家具を細かくしたら遅い端末で 50ms/フレーム | 面の数が 3 倍以上になり、塗りとふち取りの回数が増えた | 小さい面は描かない・ふちも描かない。33.7ms → 31.0ms。**1x より下の解像度も試したが、測ったら速くならなかったので入れていない** |
| 線を 1 本ずつ描くと遅い | グリッド 34 本・軌跡 260 本ぶんの描画命令が飛ぶ | 同じ色の線は 1 つの path にまとめて 1 回で塗る |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する (`tools/make-icon.js`) |
| マルチタッチのテストで、離していない指が離れる | CDP の `touchEnd` に渡すのは「離した点」。残っている点ではない | ハーネス側を直す。アプリは正しかった |
| 荷物を置いた瞬間に、また拾ってしまう | 下ろしたあともそのまま下りていて、拾う条件を満たし続けていた | 一度しっかり上がるまで拾えなくする (`rearmed`) |
| 電池が尽きて降りた直後に、また浮く | 強制着陸の処理を「飛んでいるとき」だけにしていた | 地上にいるときも効かせる |
| 遅れて出すお知らせが、次の課題に移ったあとに落ちる | 発火時に `app.ghost` などを見にいっていた | 予約したときの値を捕まえておき、入れかわっていたら出さない (結果画面と同じ) |
| テーブルの下をくぐれない | 当たり判定が球だった。厚みが直径ぶんあることになる | 平たい円柱 (横は半径・縦は厚み) で当てる |
| 暗い部屋で、どちらを向いても赤と白が両方見える | LED を裏側まで描いていた (奥行きの判定がない) | 機首とカメラの内積で、向いている側だけ光らせる |
| 灯りのにじみが霧に見える | 外側の輪まで濃く塗っていた | 外は薄く広く、中は濃く小さく |
| 8 の字の輪が読めない | 交差点に輪を 2 つ重ねて置いていた | 手前と奥に輪を分け、そのあいだで交差させる |
| 輪が画面に映っているのに矢印も出て、番号が二重になる | 「見えているか」の判定に余白を入れていた | 判定は余白なし。矢印を置く位置にだけ余白を使う |
| 広場の目印が「トーテムポール」に見える | コーンを一直線に並べていた。操縦者から見ると重なる | 千鳥に置く。スラロームの練習にもなる |
| 関数を分けたら、参照していた変数がスコープから外れて落ちた | `drawScene` を `drawScene` + `drawInterior` に割ったとき、引数だった `override` が残っていた | 分けたら、使っている変数を引数に渡す。ブラウザのテストが拾った |
| 観葉植物が「緑のタンス」に見える | 1 つの箱で描いていた | 鉢 + 幹 + 葉を数枚、高さと向きを変えて散らす。幹は葉のいちばん上まで通す |
| 棚に並べた本が見えない / 前に出すと飛び出して見える | 本を小さな箱で作っていた。奥から順に塗るだけなので、中に入れれば隠れ、出せば浮く | 本棚は蓋をした 1 つの箱にして、**前の面に色を塗る** (`room.decals`)。面も減る |
| 画面の明るさを測るテストが灯りを見落とす | 13 画素おきに間引いていた。LED は数十画素しかない | 灯りの色を数えるところだけ全画素を見る |

**新しく踏んだら、この表に足すこと。**

---

## 遅らせて出すものの決まり

結果画面・ゴーストのお知らせ・トーストなど、**あとから出るもので 3 回同じバグを踏んだ。**
やることは毎回同じ:

1. 予約するときに「どの走行 / どのデータに向けたものか」を変数に捕まえる
2. 発火したときに、それが今も現役かを確かめる
3. 入れかわっていたら、黙って何もしない

`app.run` や `app.ghost` を発火時に読みにいかないこと。
