# ドローン操作練習

家にあるトイドローン (E88/E99 系の折りたたみ機) を、部屋の中で
**操縦者から見た視点**で練習するシミュレータ。iPhone の Safari で動く。

実機がうまく飛ばせない理由は、だいたいこの 3 つ。ここを叩き込むのが目的:

1. **傾き = 加速度。** 前に倒すと「前に進む」のではなく「前に加速し続ける」。
   止めるには逆に倒す (当て舵)
2. **対面だと左右が逆。** 機体がこちらを向いていると、右に倒すと自分から見て左へ行く
3. **戻す癖がない。** 入れっぱなしにすると壁か天井に当たる

FPV (機体のカメラ視点) にはしない。上の 3 つはどれも操縦者視点でしか練習にならない。

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト
npm run test:bundle # 1 枚にまとめた版を、同じテストにかける
npm run test:all   # 上の 3 つ全部
npm run bundle     # dist/dorone.html を作る (配布・Artifact 用)
npm run icon       # apple-touch-icon.png を作りなおす
```

`npm run test:ui` は本物のブラウザを立ち上げ、**指の操作をそのまま再現する**
(マルチタッチも)。画面を触ったときの不具合はこちらでしか捕まらない。
変更したら必ず両方流す。`--slow` を付けると CPU を 4 倍遅くしたときも測る。

playwright はグローバルにあれば拾う。無ければ `npm i -D playwright`。

## 構成

```
index.html / styles.css   画面
core.js                   物理と 3D の計算。DOM を触らない
tasks.js                  練習課題・採点・失敗原因の診断。DOM を触らない
app.js                    操作と描画
core.test.js              core のテスト (node --test)
tasks.test.js             tasks のテスト (node --test)
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
tools/bundle.js           全部を 1 枚の HTML にまとめる
tools/make-icon.js        apple-touch-icon.png を作る (iOS は SVG 不可)
```

ファイルはすべて直下。スマホやタブレットからでも 1 ファイルずつ差し替えられるようにするため。

`window.__app` はテストから中身をのぞく入口。`simulate()` で時間を早送りでき、
`bench` から描画などを部品ごとに計測できる。

---

## 決めごと

### 座標と単位

x = 右、y = 上 (高さ)、z = 奥。単位は m / s / rad。
機首方位 `yaw = 0` で機首は +z (部屋の奥) を向き、増やすと右に回る。
部屋は 5.2m × 6.0m × 2.4m。操縦者 (カメラ) は (0, 1.55, -0.75) に立っている。

### 機体のモデル (手持ちの機体に寄せてある)

- **気圧センサーの高度維持あり。** スロットルは「上下の速度」の指示。戻すとその高さで止まる
- **自動水平 (アングルモード)。** スティックを戻すと機体は水平に戻る。
  ただし水平に戻っても**止まらない**
- **GPS も光学センサーもない。** トリムずれで一方向に流れ続ける。
  ここが実機で一番効くクセなので、必ず入れる
- 高度維持を切る「上級」モードもある。そのときスロットルは推力そのものになり、
  スティックはバネで戻らなくなる (実機の送信機と同じ)

実測した数字 (`npm test` が見張っている):

| | |
| --- | --- |
| 最大の傾き 20° での終端速度 | 約 2.2 m/s |
| 全開から手を離して止まるまで | 約 2.5 秒 / 1.5m 惰性で進む |
| ワンキー離陸の落ちつく高さ | 約 1.1m |
| 無操作で 60cm の輪から出るまで (ふつう) | 平均 9 秒 |

### 描画

Canvas 2D だけ。3D は「点を回して透視投影 → 奥から順に塗る」。ライブラリは使わない。

- 部屋の外殻 (床・天井・壁) を先に描き、そのあと中のもの (家具・輪・機体) を
  奥行き順に描く。中にいるので、この順なら必ず正しくなる
- **影は必ず描く。** 画面で高さを読む唯一の手がかり
- 画角は「見える範囲の対角」で決める。縦だけ・横だけで決めると、
  縦長の画面で極端な望遠になる
- カメラは目で追うようにゆっくり機体を向く。デッドゾーンの中は動かさない
  (動いている感じを残すため)。ただし**最後に固い上限**をかけて画面から出さない

---

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **ロジックを画面から切り離す。** `core.js` と `tasks.js` は DOM を触らない。
  だから物理も 3D の計算も node でテストできる
- **ビルドを挟まない。** 素の HTML / CSS / JS

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  ここで作ったときも、テスト 31 件が全部通った状態で
  「機体が画面に映っていない」「影が家具に隠れる」が残っていた
- **体感の問題は計測する。** 憶測で直さない
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅と、遅い端末でも見る。** `--slow` で CPU を 4 倍遅くする

### 3. テストプレイしてもらう

1. `npm run bundle` で `dist/dorone.html` を作る
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm run test:all` を流す**

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 縦画面が極端な望遠になる (水平 31°) | 画角を縦だけで決めていた。縦長の画面では横がすごく狭くなる | 「見える範囲の対角」で決める |
| 速く動かすと機体が画面から出る | カメラの追従がゆっくりなので、角速度が大きいと追いつけない | デッドゾーンとは別に、**固い上限**を最後にかける |
| 離陸すると機体が画面の上に飛ぶ | 最初に床のドローンを見おろしていて、上がるほど遅れが出た | 最初から「これから浮く高さ」を見ておく |
| 機体の手前にいるのに影が家具の裏に回る | 影を床と一緒に描いていた | 影と機首の矢印は、家具と同じ列に入れて奥行き順に描く |
| ヒントの吹き出しが機体を隠す | HUD の上に出していた | スティックのすぐ上に出す。HUD 全体にスティックぶんの余白を取る |
| 採点のバーが伸びない | `span` はインライン要素。`width`/`height` の % が効かない | `display: block` を付ける |
| やり直した直後にエラーで落ちる | 結果画面を遅らせて出すあいだに次の走行が始まり、まだ採点していない走行を表示しようとした | どの走行の結果かを覚えておき、入れかわっていたら出さない |
| 離陸した最初の 1 フレームで「着地した」ことになる | 地上 (y=0) から浮いた瞬間に床との接触判定が立つ | 一度しっかり浮いた機体だけが着地できる (`airborne`) |
| 天井に描いた照明が「描画の失敗」に見える | 画面の上端は遠近が強くかかり、四角がゆがむ | 天井は素のままにする |
| 遅い端末で 21fps しか出ない | canvas の画素数が支配的 (自分の JS は 3ms しか使っていなかった) | fps を見て**描く細かさを自動で下げる** (2x → 1.5x → 1x)。実測 47ms → 17ms |
| 線を 1 本ずつ描くと遅い | グリッド 34 本・軌跡 260 本ぶんの描画命令が飛ぶ | 同じ色の線は 1 つの path にまとめて 1 回で塗る |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する (`tools/make-icon.js`) |
| マルチタッチのテストで、離していない指が離れる | CDP の `touchEnd` に渡すのは「離した点」。残っている点ではない | ハーネス側を直す。アプリは正しかった |

**新しく踏んだら、この表に足すこと。**
